# Core dependencies
pandas>=1.4.0
qrcode[pil]>=7.3.0
Pillow>=9.0.0
jinja2>=3.1.0

# CLI utilities
typer[all]>=0.9.0

# Optional dependencies for PDF generation
# Note: You also need pandoc to be installed on your system
# For macOS: brew install pandoc
# For Ubuntu/Debian: sudo apt-get install pandoc
# For Windows with Chocolatey: choco install pandoc